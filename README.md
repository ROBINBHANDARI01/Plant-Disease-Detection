# Plant-Disease-Detection
This project is a Plant Disease Detection System built using Deep Learning (TensorFlow) and deployed with an interactive Streamlit web interface. It allows users to upload a plant leaf image, and the model predicts the type of disease (if any) based on visual symptoms.
